---
description: Debug command.
---
# /debug
V45.0 OOM-Shield: Autocorreção Cega. Do not ask for logs, use `grep_search` and native `bash/pwsh` to find them. Edit the file directly. Never ask for permission to test.
